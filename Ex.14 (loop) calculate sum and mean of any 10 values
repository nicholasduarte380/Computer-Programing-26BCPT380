#include<stdio.h>

int main()
{
    int i,n,sum=0;
    int mean;

    for(i=1; i<=10; i++)
    {
        printf("Enter value %d: ",i);
        scanf("%d",&n);

        sum=sum+n;
    }

    mean=sum/10;

    printf("Sum = %d\n",sum);
    printf("Mean = %d",mean);

    return 0;
}