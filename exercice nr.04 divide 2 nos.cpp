#include <stdio.h>
main ()
{
    int a,b,c;
    /* Divide 2 nos. */
    printf("Enter two nos.");
    scanf("%d %d",&a,&b);
    c = a / b;
    printf("%d / %d = %d\n",a,b,c);
    return 0;
}
